bugs:
* wall render bug, shown here: MAP=deep_sea.map
* also the side walls shuold probably use a different texture?

tech:
* spatial audio panning/attenuation (M8.2)

debt:
* kill init_default_tile_defs
* replace fallback texture with orange checkerboard
